extends layout.pug

block content
    div(class="errorForm")
        h4 Something has gone wrong!
        h5 Error:
        p=message
        h5 Click below to go back:
        a.left.loginAnchErrorPage(href="/login") Log In
        a.left(href="/registration") Register
        br
        br    